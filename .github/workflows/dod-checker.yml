name: DoD checker

# Al marcar un PR listo para revisión (o al actualizarlo), verifica la
# Definition of Done y deja el veredicto como comentario del PR + Discord.
# Nota: en un PR abierto, "PR mergeada" y "deploy a staging" quedan como
# PENDIENTE (se confirman recién al mergear/deployar). Verifica el resto.
# Usa un LLM OpenAI-compatible (default Gemini free tier) vía .github/scripts/llm.sh.

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [dev, main]

permissions:
  contents: read
  pull-requests: write
  checks: read

jobs:
  dod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Recolectar estado del PR (diff, checks, título)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr view "$PR" --json title,headRefName,mergeable,state > meta.json
          gh pr checks "$PR" > checks.txt 2>/dev/null || echo "(sin checks de CI todavía)" > checks.txt
          gh pr diff "$PR" > diff.txt || echo "(sin diff)" > diff.txt

      - name: Armar el prompt
        run: |
          {
            echo "Sos el DoD Checker de SportMatch. Verificá la Definition of Done de este PR (AGENTS.md §6) con la evidencia de abajo. NO cierres nada: reportás."
            echo "Criterios: 1) PR mergeada a dev (en un PR abierto => marcá ⏳ PENDIENTE hasta merge), 2) Tests verdes en CI (usá los checks), 3) Criterios de aceptación del RF asociado cumplidos (deducí el RF del título/rama y evaluá contra el diff; es el punto clave), 4) Doc actualizada si aplica, 5) Deploy a staging (⏳ PENDIENTE si no hay evidencia), 6) Sin deuda nueva (TODOs sin ticket, código muerto)."
            echo "Salida: tabla por criterio con ✅/❌/⏳/⚪N-A y evidencia, + veredicto final (LISTO PARA CERRAR / NO CUMPLE DoD + qué falta). Español, breve."
            echo ""
            echo "=== META ==="; cat meta.json
            echo ""
            echo "=== CHECKS CI ==="; cat checks.txt
            echo ""
            echo "=== DIFF ==="; head -c 60000 diff.txt
          } > prompt.txt

      - name: Evaluar DoD (LLM OpenAI-compatible, default Gemini)
        id: ai
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: |
          OUT=$(bash .github/scripts/llm.sh prompt.txt)
          {
            echo 'response<<EOF_LLM'
            echo "$OUT"
            echo 'EOF_LLM'
          } >> "$GITHUB_OUTPUT"

      - name: Comentar el PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
          RESPONSE: ${{ steps.ai.outputs.response }}
        run: |
          printf '### ✅ Definition of Done — chequeo automático\n\n%s\n\n---\n*Chequeo asistido por IA. La decisión de cerrar es humana.*' "$RESPONSE" > body.md
          gh pr comment "$PR" --body-file body.md

      - name: Avisar por Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_QA }}
          RESPONSE: ${{ steps.ai.outputs.response }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: |
          python3 - <<'PY'
          import json, os, urllib.request
          msg = f"✅ DoD check del PR {os.environ['PR_URL']}\n\n" + os.environ["RESPONSE"]
          data = json.dumps({"content": msg[:1900]}).encode()
          req = urllib.request.Request(os.environ["DISCORD_WEBHOOK_URL"], data=data,
                                       headers={"Content-Type": "application/json", "User-Agent": "Mozilla/5.0 (compatible; SportMatchBot/1.0)"})
          urllib.request.urlopen(req)
          PY

# Secrets: LLM_API_KEY (Gemini u otro OpenAI-compatible), DISCORD_WEBHOOK_QA
# Variables opcionales: LLM_BASE_URL, LLM_MODEL (ver .github/scripts/llm.sh)
