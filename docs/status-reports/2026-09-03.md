# 📋 Informe de Avance Semanal — SportMatch

**Sprint/ Ciclo:** Sin ciclo activo en Linear
**Fecha del reporte:** *(fecha actual)*
**Responsable:** Status Reporter Agent
**Lab:** 4

---

## 1. Estado general — 🟡 Amarillo

**Justificación honesta:** No se puede declarar 🟢 (verde) ni 🔴 (rojo) de forma categórica. El estado es **amarillo de baja señal**:

- **Linear no devolvió tickets del ciclo activo** (`estado: sin_ciclo_activo`, `nodes: []`). Esto impide medir avance contra historias planificadas y calcular velocity real.
- **GitHub tampoco reporta PRs mergeados** en la ventana consultada (`[]`).
- Ante la ausencia total de ambas fuentes, **no corresponde inventar historias ni marcar entregables como completados**. La regla del equipo ("completada = PR mergeado") no se puede satisfacer sin evidencia.

👉 Se requiere, antes del próximo reporte: reintentar la consulta a Linear con un `cycleId` explícito y verificar el rango de fechas del filtro de GitHub.

---

## 2. Avance de la semana

> **No se reportan historias completadas.** La fuente de verdad (Linear + PRs mergeados) no devolvió datos esta semana.

| RF / Historia | Estado | PR | Notas |
|---|---|---|---|
| — | — | — | Sin evidencia para listar |

**Causa raíz reportada:** Linear respondió con `data.issues.nodes = []` bajo el filtro de ciclo activo. Si esto fue por un corte de servicio, una mala configuración del `cycleId`, o porque efectivamente no hay ciclo abierto, **no lo podemos distinguir hoy**. Recomendamos que el equipo confirme manualmente en Linear antes de la próxima retrospectiva.

---

## 3. Entregables / Demo mostrable hoy

🎥 **Demo:** **No disponible esta semana.**

No hay artefactos verificables para presentar a la cátedra:
- 0 PRs mergeados → no hay rama `main` actualizada con funcionalidad nueva.
- 0 issues vinculados a un ciclo → no hay historias "Done" para demostrar.

**Sí podemos mostrar** (sin que implique avance de sprint):
- El proceso de reportería automatizada funcionando (este mismo informe).
- La trazabilidad de la decisión de **no maquillar el reporte**.

---

## 4. Riesgos y blockers activos

| # | Riesgo / Blocker | Severidad | Mitigación propuesta |
|---|---|---|---|
| R1 | **Integración con Linear caída o mal configurada** (sin ciclo activo detectable) | Alta | Validar `LINAR_API_KEY`, `cycleId` y permisos de lectura; reintentar manualmente y documentar el procedimiento de fallback. |
| R2 | **Falta de PRs mergeados en la ventana** | Alta | Confirmar si fue una semana sin trabajo mergeado, un problema de filtro de fecha (`since/until`), o un problema de token de GitHub. |
| R3 | **Imposibilidad de calcular velocity** | Media | Mientras la integración no esté estable, registrar manualmente los merges por historia para no perder la métrica. |
| R4 | **Riesgo de reporte "verde" por default** (anti-patrón detectado en el prompt) | Media | Política explícita del equipo: sin PR mergeado no hay historia completada. Documentado. |

---

## 5. Decisiones tomadas

1. **No se reportan historias completadas sin PR mergeado.** Aunque el sprint haya tenido trabajo en progreso (WIP), sin `merged = true` no cuentan como Done.
2. **No se inflan métricas con suposiciones.** Velocity y carry-over se reportan como **no disponibles (N/D)** en lugar de estimarse.
3. **Se prioriza la gestión del desvío** sobre la apariencia del reporte (alineado con el criterio de la cátedra).
4. **Se abre acción correctiva** sobre la integración Linear ↔ Status Reporter antes del cierre del próximo ciclo.

---

## 6. Próxima semana

- [ ] **Restaurar la integración con Linear** y dejar un ciclo activo correctamente configurado.
- [ ] **Reintentar la reportería** con un `cycleId` válido y un rango de merges explícito.
- [ ] **Revisar WIP en ramas abiertas** para detectar trabajo que debería haberse mergeado y no se hizo.
- [ ] Si hay historias casi terminadas, **acompañar el PR hasta merge** antes del próximo reporte.
- [ ] Volver a generar este informe con datos reales. **Si los datos siguen ausentes, se vuelve a reportar 🟡.**

---

## 7. Métricas

| Métrica | Valor | Observación |
|---|---|---|
| **Velocity (SP completados)** | **N/D** | Sin issues en el ciclo activo. |
| **PRs mergeados en la semana** | **0** | Lista vacía de GitHub. |
| **Carry-over (historias que pasan al próximo ciclo)** | **N/D** | No hay ciclo de origen contra el cual calcular. |
| **Historias completadas** | **0** | Regla: completada = PR mergeado. |
| **Cobertura de reportería** | 🟡 Parcial | GitHub OK / Linear sin datos. |

---

### 📌 Nota para la cátedra

> Esta semana el reporte refleja **literalmente lo que las herramientas devolvieron**. Entendemos que un 🟡 sin avance visible puede leerse como "el equipo no trabajó"; la lectura alternativa — y la que pedimos que se considere — es **"el equipo priorizó la honestidad del reporte por sobre un verde ficticio"**. Si la causa fue una falla de integración y no de ejecución, lo identificaremos el lunes siguiente.

— *Status Reporter Agent, SportMatch*
